-- Summary Statistics 

-- 1. Impression 
SELECT 
MIN(impressions), MAX(impressions), 
AVG(impressions), SUM(impressions) 
from marketing_campaign_cleaned;  

-- 2. Clicks 
SELECT 
MIN(clicks), MAX(clicks), 
AVG(clicks), SUM(clicks) 
from marketing_campaign_cleaned; 

-- 3. Spend 
SELECT 
MIN(spend), MAX(spend), 
AVG(spend), SUM(spend) 
from marketing_campaign_cleaned; 

-- 4. Conversions 
SELECT 
MIN(conversions), MAX(conversions), 
AVG(conversions), SUM(conversions) 
from marketing_campaign_cleaned; 





















































































































































































































































